# ==============================================================================
# Verifyr RAG System - Environment Variables
# ==============================================================================
#
# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to version control!
#
# Usage: cp .env.example .env

# ==============================================================================
# LLM API Keys
# ==============================================================================

# Anthropic Claude API
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API (for GPT models)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# ==============================================================================
# Langfuse Configuration (Observability & Evaluation)
# ==============================================================================

# Langfuse Host URL
# For local Docker setup: http://localhost:3000
# For Langfuse Cloud: https://cloud.langfuse.com
LANGFUSE_HOST=http://localhost:3000

# Langfuse Public Key
# Get from Langfuse dashboard: Settings → API Keys
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key_here

# Langfuse Secret Key
# Get from Langfuse dashboard: Settings → API Keys
LANGFUSE_SECRET_KEY=your_langfuse_secret_key_here

# ==============================================================================
# Docker Compose Configuration (for Langfuse)
# ==============================================================================

# Next Auth Secret (for Langfuse authentication)
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-change-this-in-production

# Salt for encryption
# Generate with: openssl rand -base64 32
SALT=your-salt-change-this-in-production

# ==============================================================================
# Application Settings (Optional)
# ==============================================================================

# API Server Port (default: 8000)
# PORT=8000

# Log Level (debug, info, warning, error)
# LOG_LEVEL=info

# Enable debug mode (true/false)
# DEBUG=false

# ==============================================================================
# Notes
# ==============================================================================
#
# 1. LANGFUSE SETUP (Local Docker):
#    - Run: docker-compose up -d
#    - Access: http://localhost:3000
#    - Create account (first-time only)
#    - Go to Settings → API Keys
#    - Copy Public Key and Secret Key to this file
#
# 2. LANGFUSE CLOUD (Alternative):
#    - Sign up at: https://cloud.langfuse.com
#    - Set LANGFUSE_HOST=https://cloud.langfuse.com
#    - Get API keys from dashboard
#
# 3. GENERATE SECRETS:
#    openssl rand -base64 32
#
# 4. NEVER commit .env to git!
#    The .env file is in .gitignore by default
#
